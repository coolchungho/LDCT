# 生產（K8s Ingress 將 /api 轉至後端時）：僅提供 SPA；API 與 UI 同源由 Ingress 路由。
server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }
}
